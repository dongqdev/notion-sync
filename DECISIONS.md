# 결정 로그

왜 이렇게 만들었는지 나중에(또는 다른 세션이) 다시 묻지 않도록 기록. 결정을 뒤집을 땐
지우지 말고 "번복" 항목을 새로 추가하고 이전 항목에 링크할 것 — 왜 바뀌었는지도 히스토리로
남기기 위함.

---

### 2026-08-16 — 웹 UI(React/Express/Vite)를 제거하고 CLI 전용으로 전환

- **결정**: `src/`, `server/`, 루트 `index.html`, `vite.config.js`를 전부 삭제하고
  `package.json`에서 react/express/vite 등 웹 전용 의존성을 뺐다.
- **왜**: 실제 사용 패턴은 브라우저 클릭이 아니라 Claude Code 같은 에이전트가 `notion-cli`를
  직접 실행하는 것이었다(README에도 이미 "진짜 사용법은 웹 화면 클릭이 아니라..."라고
  명시돼 있었음). CLI는 Notion SDK(`@notionhq/client`)를 직접 호출해서 서버가 필요 없고,
  웹 UI의 "AI 생성" 기능은 고정 템플릿을 반환하는 데모일 뿐 실제 생성 로직이 아니었다
  (`.agents/skills/notion-ai-tool/SKILL.md`에도 스킬 흐름에서는 안 쓴다고 명시).
- **다시 볼 조건**: 브라우저 UI로 직접 조작해야 하는 요구가 새로 생기면 재검토.

### 2026-08-16 — `findPageByTitleExact`를 "포함" 매치에서 "완전 일치 우선 + 애매하면 거부"로 변경

- **결정**: 페이지 제목이 검색 쿼리를 부분 문자열로 포함하기만 해도 매치로 인정하던 걸,
  (1) 완전 일치(대소문자 무시)를 우선 확인하고, (2) 없으면 "포함" 매치 후보가 정확히 하나일
  때만 허용, (3) 둘 이상이면 에러로 거부하도록 바꿨다.
- **왜**: 이 도구 자신의 네이밍 컨벤션(`[백업 원문] <제목> (날짜)`) 때문에, 원본 페이지
  제목이 자기 백업 페이지 제목의 부분 문자열이 되는 경우가 흔하다 — 실제로 `delete`가 원본
  대신 백업 페이지를 지운 사고로 확인됨(ISSUES.md 2026-08-16 참고).
- **다시 볼 조건**: 없음(라이브로 재현 후 재검증 완료).
