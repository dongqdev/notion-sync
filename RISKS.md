# RISKS

구조적으로 완전히 제거할 수 없는 잔존 위험, 또는 아직 안 터졌지만 예상되는 문제. "고칠
것"이 아니라 "계속 지켜볼 것" 목록. 실제로 터지면 [ISSUES.md](ISSUES.md)로 옮긴다.

---

### Notion API 반복 호출에 429 재시도/백오프가 없음

- **증상 예상**: `appendBlocksInBatches`, `approve-edit`의 블록 삭제 루프 등 여러 번 반복
  호출하는 곳 어디에도 rate limit(429) 재시도 로직이 없다. 긴 문서를 처리하다 Notion API
  요청 한도에 걸리면 그 자리에서 에러로 중단된다.
- **영향**: `approve-edit`은 이미 원문을 백업한 뒤 삭제 단계에서 걸릴 수 있는데, 이 경우
  `APPROVED_EDIT_PARTIAL`로 실패를 알려주긴 하지만(무시하지 않음), 자동 재시도는 안 된다 —
  사용자/에이전트가 결과 메시지를 보고 수동으로 다시 시도해야 함.
- **제안**: 429 응답에 지수 백오프 재시도를 붙이는 걸 고려. 지금 규모(개인 도구, 짧은 실행)
  에서는 급하지 않음.
- **상태**: 관찰 중.
