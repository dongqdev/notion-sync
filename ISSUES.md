# ISSUES

알려졌지만 아직 안 고친 결함/버그 목록. 새 이슈는 위에 추가하고, 고치면 지우거나 완료
표시. 관련 문서: [RISKS.md](RISKS.md)(예상 문제), [DECISIONS.md](DECISIONS.md)(설계 결정
이유).

---

## 2026-08-16

### [해결] `delete`가 원본 대신 백업 페이지를 지울 수 있었음

- **증상**: `findPageByTitleExact`가 "제목에 쿼리가 포함되는지"만 확인해서, 백업 페이지
  제목(`[백업 원문] <원본 제목> (날짜)`)이 원본 제목을 부분 문자열로 포함하는 경우 둘 다
  매치 후보가 됐고, 검색 순위상 앞선 쪽이 조용히 선택됐다. 라이브 테스트 중 실제로 `delete`가
  원본이 아니라 방금 만든 백업 페이지를 archived 처리하는 사고로 재현됨.
- **조치**: 완전 일치(대소문자 무시)를 우선 확인하고, 없으면 "포함" 매치 후보가 정확히
  하나일 때만 허용, 둘 이상이면 `Ambiguous: ...` 에러로 거부하도록 `findPageByTitleExact`
  수정. 라이브로 재현 후 재검증 완료.
- **상태**: 해결.

### [해결] `propose-edit`을 승인 없이 두 번 호출하면 두 번째 제안이 유실될 수 있었음

- **증상**: `approve-edit`이 배너를 `findIndex`(첫 번째 매치)로만 찾아서, 미승인 배너가
  2개 이상이면 첫 배너 이전만 백업·삭제하고 두 번째 제안 내용은 페이지에 그대로 남았다.
  이 상태에서 나중에 다시 `approve-edit`을 호출하면 두 번째 제안 내용이 "원문"으로
  잘못 백업될 수 있는 구조였음.
- **조치**: 배너가 2개 이상이면 `MULTIPLE_PROPOSALS_FOUND`로 거부하고 수동 정리를 안내하도록
  변경. 라이브로 propose-edit 2회 연속 호출 후 approve-edit이 정상적으로 거부하는 것까지
  확인.
- **상태**: 해결.

### [해결] 삭제/편집을 반복하면 백업·기록 페이지 제목에 날짜가 계속 누적됨

- **증상**: 같은 페이지를 여러 번 delete/approve-edit 처리하면 제목 끝에
  "(2026-08-16) (2026-08-16) (2026-08-16)..."처럼 날짜가 계속 붙었다.
- **조치**: 새 날짜 접미사를 붙이기 전에 기존 trailing date 패턴을 제거하는
  `stripTrailingDateSuffix`를 추가.
- **상태**: 해결.

### [해결] 긴 문단/코드블록이 Notion의 rich_text 2000자 제한을 넘으면 API 에러

- **증상**: `lib/markdown-to-blocks.js`가 텍스트 길이를 체크하지 않아서, 문단이나 코드블록이
  2000자를 넘으면 그대로 하나의 `rich_text.text.content`로 보내다 Notion API가 통째로 거부.
- **조치**: 2000자 단위로 자동 청크 분할(`chunkText`)해서 여러 개의 연속 텍스트 런으로
  나눠 보내도록 수정.
- **상태**: 해결.
